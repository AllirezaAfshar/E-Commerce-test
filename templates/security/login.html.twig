{% if error %}
    <div>{{ error }}</div>
{% endif %}

{{ form_start(form) }}
    {{ form_row(form.username) }}
    {{ form_row(form.password) }}


<button type="submit">Login!</button>
{{ form_end(form) }}